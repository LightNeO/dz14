[pytest]
testpaths = tests
pythonpath = .
addopts = -v --tb=short

markers =
    wifi: tests for the station_WiFi firmware
    ble: tests for the SENTRY-BLE firmware
    hw: test requires a physical ESP32-S3 DUT
    slow: long-running hardware test

